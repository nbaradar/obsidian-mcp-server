[project]
name = "obsidian-vault"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.19.0",
    "pyyaml>=6.0.1",
    "python-frontmatter>=1.1.0",
]
